// Place your settings in this file to overwrite the default settings
{
    "files.exclude": {
        "dist": true,
        "dist-server/express-server": true,
        "**/*.js": {"when": "$(basename).ts"}
		"**/*.js.map": true
    }
}